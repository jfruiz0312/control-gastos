spring:
  application:
    name: app.management
  profiles:
    active: dev

server:
  servlet:
    context-path: /

springdoc:
  api-docs:
    path: /api-docs
  swagger-ui:
    path: /swagger-ui.html